#include "VulkanPipeline.hpp"

void VulkanPipeline::createPipeline(Shader<ShaderType::VERTEX> vertex, Shader<ShaderType::FRAGMENT> fragment)
{
    // VkShaderModule vertM = vertex.get();
    // VkShaderModule fragM = fragment.get();


    // vkDestroyShaderModule(device, fragShaderModule, nullptr);
    // vkDestroyShaderModule(device, vertShaderModule, nullptr);

}